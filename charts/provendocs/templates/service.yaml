apiVersion: v1
kind: Service
metadata:
  name: provendocs-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: provendocs
  ports:
    - name: provendocs-port
      port: {{ .Values.http.port }}
      targetPort: {{ .Values.http.port }}
      protocol: TCP