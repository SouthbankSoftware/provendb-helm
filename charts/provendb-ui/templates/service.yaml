apiVersion: v1
kind: Service
metadata:
  name: provendb-ui-{{ .Release.Name }}-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: provendb-ui-{{ .Release.Name }}
  ports:
    - name: provendb-ui-{{ .Release.Name }}-port
      port: {{ .Values.http.port }}
      targetPort: {{ .Values.http.port }}
      protocol: TCP