apiVersion: v1
kind: Service
metadata:
  name: provendb-api-gateway-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: provendb-api-gateway
  ports:
    - name: http-port
      port: {{ template "port" . }}
      targetPort: {{ template "port" . }}
      protocol: TCP