apiVersion: v1
kind: Service
metadata:
  name: proxy-{{ .Release.Name }}-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: proxy-{{ .Release.Name }}
  ports:
    - name: proxy-{{ .Release.Name }}-port
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.proxy.port }} 
      protocol: TCP
  type: {{ .Values.service.type }}
