provendb-proxy:
  enabled: true
  parent: true #Don't change this. This will be used to decide if proxy service should bind to an existing static IP or should be allocated a new one
  externalIpStaging: "35.189.19.35" #static ip. If running multiple helm releases at once, the IP needs to change
  externalIpProduction: ""

provendb-tree:
  enabled: true

provendb-user:
  enabled: true
  
provendb-api-gateway:
  enabled: true
  oauth:
    redirect:
      host: apigateway.provendb.com
  user:
    domain:
      name: usermodule.provendb.com
  domain:
    name: apigateway.provendb.com
  allowed:
    urls: http://provendocs.com/login

provendocs:
  enabled: true
  env: prod

provendb-concierge:
  grpc:
    port: 10002
  prometheus:
    port: 10003  
  enabled: true

provendb-io:
  enabled: true 
  grpc:
    port: 10004
  prometheus:
    port: 10005 

provendb-ui:
  enabled: true
  http:
    port: 8088


global:
  jaegeragent:
    host:
      port: "jaeger-agent.istio-system.svc.cluster.local:6831"    


   


    